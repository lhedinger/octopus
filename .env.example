# Optional: enable AI-generated microservice art.
# Point this at the deployed proxy in api/generate-image.ts.
# Leave unset to use procedural art only (the default).
# VITE_AI_IMAGE_URL=/api/generate-image

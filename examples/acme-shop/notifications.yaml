octopus: 1
repo: notifications
name: Notifications
kind: microservice
description: Email and push receipts
context: communication
team: platform
dependencies:
  # Not scanned — appear on the map as external-system stubs.
  - repo: ses
    label: SMTP API
  - repo: fcm
    label: push API
modules:
  - name: Email
    description: Transactional email delivery
    dependencies: [Templates]
  - name: Push
    description: Mobile push notifications
    dependencies: [Templates]
  - name: Templates
    description: Localised message templates
build:
  status: passing
  items: [lint, docker image]
test:
  coverage: 73
  items: [unit]
deploy: [staging, production]
health:
  status: degraded
environments:
  staging: { version: 2.0.0 }
  production: { version: 1.9.9, status: degraded }

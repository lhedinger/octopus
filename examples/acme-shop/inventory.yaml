octopus: 1
repo: inventory
name: Inventory
kind: microservice
description: Stock levels and reservations
context: fulfillment
team: fulfillment
storage:
  - kind: database
    name: inventory-db
modules:
  - name: Stock
    description: On-hand quantities per warehouse
  - name: Reservations
    description: Holds during checkout
    dependencies: [Stock]
build:
  status: passing
  items: [lint, unit tests, docker image]
test:
  coverage: 49
  items: [unit]
deploy: [staging, production]

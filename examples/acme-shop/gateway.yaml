octopus: 1
repo: gateway
name: Edge Gateway
kind: apiGateway
description: Routing, auth, rate limiting
context: experience
dependencies:
  - repo: catalog
  - repo: orders
  - repo: payments
# Plain-list shorthand still works: items only, no status/coverage.
build: [lint, docker image]
test:
  coverage: 55
deploy: [staging, production]

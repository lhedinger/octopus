octopus: 1
repo: catalog
name: Catalog
kind: microservice
description: Products, categories, pricing
context: discovery
storage:
  - kind: database
    name: catalog-db
dependencies:
  - repo: search
    kind: async
    label: product.updated
build:
  status: passing
  items: [lint, unit tests, docker image]
test:
  coverage: 64
  items: [unit, pricing rules suite]
deploy: [staging, production]

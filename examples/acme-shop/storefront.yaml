octopus: 1
repo: storefront
name: Storefront
kind: client
description: Web and mobile shop UI
context: experience
dependencies:
  - repo: gateway
    label: REST
build:
  status: passing
  items: [lint, type check, bundle]
test:
  coverage: 78
  items: [unit, visual regression]
deploy: [preview, production]
